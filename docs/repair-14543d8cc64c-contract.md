# Overview

This document defines the normative repair contract for governed run `14543d8cc64c`. In governed run `14543d8cc64c`, execution stalled and failed during test execution of `make test` and `pytest` for the `sysdiff` utility vertical slice. The origin run encountered execution timeouts and missing external toolchain dependencies, specifically `clang` and `cppcheck`, which prevented the validation suite from completing successfully within orchestrator resource limits.

The primary purpose of this repair slice is to establish robust execution bounds, prevent timeouts during quality checks, and ensure graceful, fail-safe handling of tool dependencies when optional compilers or static analyzers are not installed in the environment, all while preserving the strict quality floor required by repository operating rules.

The required outputs of this repair slice are:
1. `docs/repair-14543d8cc64c-contract.md` (this repair contract, defining the problem, required outputs, constraints, validation criteria, routing intent, and acceptance checks under exact required headings).
2. `tests/user_journeys_manifest.json` (the canonical repository-owned user journey manifest oracle, adhering strictly to the canonical schema and covering all acceptance checks).
3. `journeys/user_journeys_manifest.json` (the secondary user journey manifest, synchronized identically with the canonical test oracle).
4. Verified execution mechanisms and tests ensuring that test runs and dependency probes execute predictably within allocated timeouts.

Routing intent: This repair routes test execution and tool readiness verification through fail-safe, preflight-guided paths. When running `make test` or `pytest`, test harnesses must enforce bounded execution windows and avoid unmonitored hangs. For external dependencies such as `clang` and `cppcheck`, the build and test harnesses must probe availability cleanly before invoking strict recipes. When tools are present, all strict compiler warnings, static analysis passes, and sanitizers run unconditionally; when tools are absent, the environment reports structured diagnostic skip or refusal metadata rather than timing out or triggering unhandled exit faults. All step executions must strictly confine their file writes to declared allowed paths.

# Problem

In governed run `14543d8cc64c`, the workflow failed during step execution involving `make test` and `pytest` for `sysdiff`. Two distinct but compounding failure modes occurred during execution:

1. **Test Execution Timeouts:** The repository test suite comprises over 680 tests across unit, integration, and fuzzing harnesses, in addition to shell test scripts (`tests/test_sysdiff.sh`). Under standard orchestrator execution, running the unoptimized full suite synchronously can exceed two minutes (120+ seconds) or stall when sub-processes wait indefinitely on external tool probes, shell pipes, or heavy fixture iterations. When execution time exceeds the orchestrator's step timeout threshold, the process is terminated abruptly, leaving the step in an unrecoverable failure state without structured diagnostic artifacts.

2. **Missing Toolchain Dependencies (`clang`, `cppcheck`):** The repository quality rules require comprehensive verification including GCC, Clang (`-std=c17 -Wall -Wextra -Wpedantic -Werror`), clang-format, clang-tidy, cppcheck, and sanitizers (AddressSanitizer and UndefinedBehaviorSanitizer). However, in containerized or minimal host environments where `clang` or `cppcheck` are not installed or not discoverable in `$PATH`, invoking recipes such as `make clang-strict`, `make cppcheck-check`, or sanitizer targets causes immediate failure with command not found errors, or causes test fixtures to hang while probing or waiting for subprocesses.

These issues compounded in run `14543d8cc64c`: missing tools caused sub-tasks to stall or fail uncleanly, leading to aggregate execution times that violated step timeout constraints. The repair must resolve these problems by implementing proper preflight tool discovery, graceful handling of missing external dependencies with clear diagnostic reporting, and performance bounding of test runs to ensure that `make test` and `pytest` complete reliably within allocated orchestrator timeouts.

# Constraints

1. **Strict Write Scope Confinement:** All file modifications across all steps of this repair must remain strictly within declared `allowed_paths`. For this contract definition step, writes are strictly limited to `docs`, `tests/user_journeys_manifest.json`, and `journeys`. No modifications may occur outside these designated boundaries.
2. **Quality Floor Preservation:** Under `AGENTS.md`, release-quality software must pass rigorous verification including GCC, Clang, strict flags, clang-tidy, cppcheck, and sanitizers. Preflight tool checks and dependency mitigation must never lower or bypass the quality floor on environments where tools are installed. On environments where secondary tools like `clang` or `cppcheck` are absent, the system must fail-safe with explicit, typed diagnostic feedback rather than crashing with untyped errors or hanging indefinitely.
3. **Execution Bounding and Timeout Mitigation:** Test invocations (`make test`, `pytest`) must be bounded to complete well within orchestrator time limits. Long-running or intensive tests must be scoped and managed so that synchronous runs do not exceed step timeouts or hang without output.
4. **User Journey Manifest Integrity and Synchronization:** Both `tests/user_journeys_manifest.json` and `journeys/user_journeys_manifest.json` must exist as identical parsed objects adhering to `USER_JOURNEYS_MANIFEST_SCHEMA`. All 21 journeys—comprising the 10 preserved author journeys, 4 core sysdiff product journeys, and 7 sandbox containment and repair journeys—must be preserved without omission.
5. **Traceability and Command Allowlist:** Every required user journey must map via `traces_to` to at least one valid acceptance check (`AC-1`, `AC-2`, or `AC-3`). The manifest `command_allowlist` must remain strictly `["build/sysdiff"]`.
6. **Non-Product Blast Radius:** This repair applies exclusively to governance contracts, manifest synchronization, test harness timeouts, and tool dependency handling. It must introduce zero modifications or regressions to `src/sysdiff.c`, Makefile binary packaging rules, or manual pages (`man/sysdiff.1`).
7. **Closed Hazard Taxonomy:** Hazards identified in run `14543d8cc64c` are classified under `EXECUTION_TIMEOUT` (unbounded test execution) and `TOOL_AVAILABILITY` / `ORACLE_TAMPERING` / `BLAST_RADIUS` under the established governance taxonomy. No unauthorized hazard classes may be introduced.

# Acceptance Checks

- **AC-1** — **Test Execution Timeout Mitigation and Performance Bounding:** The repair contract `docs/repair-14543d8cc64c-contract.md` is established under required headings with at least 120 non-whitespace characters per heading. Test execution harnesses for `make test` and `pytest` are verified to run within bounded execution limits, preventing timeouts and ensuring that test suites execute predictably without unmonitored hangs or stalls.
- **AC-2** — **Preflight Dependency Verification and Graceful Tool Handling:** Tool dependencies including `clang` and `cppcheck` are validated via structured preflight probing. When these tools are absent from the host environment, the system emits clear diagnostic messages and handles their absence gracefully without causing unhandled exceptions or timeout failures, while fully enforcing strict checks whenever tools are present.
- **AC-3** — **Manifest Synchronization, Test Suite Integrity, and Non-Product Blast Radius:** `tests/user_journeys_manifest.json` and `journeys/user_journeys_manifest.json` are maintained as identical parsed objects adhering to canonical schema, containing all 21 required journeys, and mapping every required journey to an enumerated acceptance check (`AC-1`, `AC-2`, or `AC-3`). The test suite passes cleanly, and zero regressions or unauthorized alterations are made to `src/sysdiff.c`, build targets, or CLI behavior.
