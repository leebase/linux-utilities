# Overview

This document defines the normative repair contract for governed run `f4ce136a16fa`. In governed run `f4ce136a16fa`, execution failed during validation at step `step_05_implement_slice`. The trusted failure evidence recorded: `Command failed: python3 -m pytest tests/test_repair_b8bf0e7c59d5.py: test_python_test_harness_decontamination`. The prior run attempted to implement slice repairs but left foreign project paths and unauthorized run-directory bypasses in the repository's test harness, causing the regression oracle `tests/test_repair_b8bf0e7c59d5.py` to fail fail-closed.

The primary purpose of this repair slice is to resolve the test harness contamination failure by excising all foreign repository paths (specifically `/home/lee/projects/employee-contract/src`) and unauthorized run-directory bypasses from the Python test suite, restoring complete test harness cleanliness, guaranteeing that `tests/test_repair_b8bf0e7c59d5.py` and all regression tests pass cleanly, maintaining synchronized user journey manifests, and upholding repository architectural boundaries, write scope confinement, and the quality floor without regressions.

The sealed evidence for the failure in governed run `f4ce136a16fa` is preserved in the sealed evidence directory:
`/home/lee/projects/linux-utilities-agent-orch-runs/f4ce136a16fa`

Required Outputs:
The required outputs of this repair slice across its execution lifecycle are:
1. `docs/repair-f4ce136a16fa-contract.md` (this repair contract, establishing the normative failure specification, required outputs, constraints, validation rules, routing intent, and acceptance checks under exact required headings).
2. `journeys/user_journeys_manifest.json` (the secondary user journey manifest, synchronized identically with the canonical test oracle and preserving all 21 required author, sysdiff, and sandbox containment journeys).
3. `tests/user_journeys_manifest.json` (the canonical repository-owned user journey manifest oracle, adhering strictly to `USER_JOURNEYS_MANIFEST_SCHEMA`, preserving all 21 required journeys, and maintaining the immutable command allowlist `["build/sysdiff"]`).
4. Python test harness decontamination and test suite repair implementation (eliminating all foreign repository paths such as `/home/lee/projects/employee-contract/src` from `sys.path` in test modules, excising unauthorized run-directory bypasses from `tests/test_commissioning_dependencies.py`, and ensuring all test modules pass cleanly).
5. Comprehensive automated regression tests under `tests/` verifying test harness decontamination, manifest synchronization, and non-product blast radius containment.

Validation:
Validation for this repair slice enforces contract completeness, test harness cleanliness, test suite passing, manifest integrity, and non-regression:
- The contract document must exist at `docs/repair-f4ce136a16fa-contract.md` with substantive text (at least 120 non-whitespace characters) under each required heading: Overview, Problem, Constraints, and Acceptance Checks.
- `tests/test_repair_b8bf0e7c59d5.py` must pass cleanly under `python3 -m pytest tests/test_repair_b8bf0e7c59d5.py`, confirming that `test_python_test_harness_decontamination` and all other regression assertions succeed without failure.
- No test file under `tests/` may contain foreign repository paths (such as `/home/lee/projects/employee-contract/src`) or unauthorized bypasses for `linux-utilities-agent-orch-runs`.
- Both `tests/user_journeys_manifest.json` and `journeys/user_journeys_manifest.json` must exist as identical parsed JSON objects conforming to `USER_JOURNEYS_MANIFEST_SCHEMA`.
- Every required journey in the manifest must trace via `traces_to` to an explicit acceptance check (`AC-1`, `AC-2`, or `AC-3`), with all acceptance checks covered.
- The existing pytest test suite and C build targets must pass cleanly without introducing product regressions or unauthorized scope expansion.

Routing Intent:
This repair routes Python test module execution, dependency resolution, and harness imports through clean, project-local and standard system environments without cross-project path leakage. Python test execution must resolve packages strictly from standard Python site packages and approved project paths (such as `/home/lee/projects/agent-orch/src`), strictly rejecting foreign project directory paths (`/home/lee/projects/employee-contract/src`). All step executions across this repair must strictly confine file mutations to declared `allowed_paths`, preventing path escape violations. All sysdiff utility invocations route strictly through `build/sysdiff` directly without wrapper escapes.

# Problem

In governed run `f4ce136a16fa`, preserved in sealed evidence directory `/home/lee/projects/linux-utilities-agent-orch-runs/f4ce136a16fa`, execution failed during slice implementation at step `step_05_implement_slice`. The trusted failure evidence recorded:
`Command failed: python3 -m pytest tests/test_repair_b8bf0e7c59d5.py: test_python_test_harness_decontamination`

The rationale for this repair contract is directly grounded in this trusted failure: Repair the trusted failure from governed run f4ce136a16fa at step step_05_implement_slice. Evidence: Command failed: python3 -m pytest tests/test_repair_b8bf0e7c59d5.py: test_python_test_harness_decontamination.

Analysis of the failure and environmental root causes:

1. **Test Harness Contamination via Foreign Project Paths:**
   In governed run `f4ce136a16fa`, execution attempted to validate the test suite against the regression oracle `tests/test_repair_b8bf0e7c59d5.py`. However, previous changes had introduced foreign repository paths into test module imports. Specifically, `tests/test_governed_run_17ca9404991a_repair.py` and `tests/test_governed_workspace_abstraction.py` injected `/home/lee/projects/employee-contract/src` directly into Python's `sys.path`. This foreign repository path belongs to an unrelated project (`employee-contract`), creating unmanaged cross-project coupling and violating isolation boundaries. When `test_repair_b8bf0e7c59d5.py` ran its decontamination check (`test_python_test_harness_decontamination`), it detected the presence of `/home/lee/projects/employee-contract` in active test modules and failed fail-closed.

2. **Unauthorized Run Directory Bypass in Dependency Validation:**
   Additionally, `tests/test_commissioning_dependencies.py` contained an unauthorized bypass for `/home/lee/projects/linux-utilities-agent-orch-runs/` to suppress baseline dependency check failures. Under repository operating rules and `AGENTS.md`, test modules must never hardcode run bypasses or poke into uncommitted run directories to satisfy validation gates. This bypass was flagged as an oracle tampering violation and failed the corresponding regression check in `tests/test_repair_b8bf0e7c59d5.py`.

3. **Failure to Decontaminate during Slice Implementation (`step_05_implement_slice`):**
   In run `f4ce136a16fa`, step `step_05_implement_slice` was tasked with implementing slice changes and ensuring that all regression gates passed. The worker in that step failed to excise the foreign path imports from `tests/test_governed_run_17ca9404991a_repair.py` and `tests/test_governed_workspace_abstraction.py`, and failed to remove the unauthorized bypass from `tests/test_commissioning_dependencies.py`. Consequently, running `python3 -m pytest tests/test_repair_b8bf0e7c59d5.py` immediately aborted on `test_python_test_harness_decontamination`, halting governed workflow execution.

4. **Preservation of Non-Product Blast Radius:**
   Under repository operating rules, this repair slice must resolve the test harness contamination strictly within the test files, governance manifests, and documentation. It must not alter production C source code (`src/sysdiff.c`), Makefile binary targets, or manual pages (`man/sysdiff.1`).

# Constraints

1. **Strict Write Scope Confinement:** All file modifications across all steps of this repair must remain strictly within declared `allowed_paths`. For this contract definition step (`step_01_define_slice_contract`), writes are strictly confined to `docs`, `tests`, and `journeys`. Step workers must never create unconfined scripts or temporary artifacts in the repository root or outside allowed paths. Any temporary notes, experimental scripts, or self-checks must reside exclusively in the orchestrator-designated scratch directory (`/home/lee/projects/linux-utilities-autonomous/.agent-orch-scratch/7581beae0012/step_01_define_slice_contract/attempt-1`).
2. **Failure Rationale Grounding and Quality Floor Preservation:** Grounded in the failure rationale: Repair the trusted failure from governed run f4ce136a16fa at step step_05_implement_slice (Evidence: Command failed: python3 -m pytest tests/test_repair_b8bf0e7c59d5.py: test_python_test_harness_decontamination). Under `AGENTS.md`, release-quality software must pass rigorous verification including strict ISO C17 compiler flags (`-std=c17 -Wall -Wextra -Wpedantic -Werror`), clang-tidy, cppcheck, sanitizers (AddressSanitizer and UndefinedBehaviorSanitizer), and Valgrind. Repairing test modules or governance manifests must never lower, disable, or bypass required quality checks or regression suites.
3. **Preservation of Sealed Evidence:** The sealed evidence directory `/home/lee/projects/linux-utilities-agent-orch-runs/f4ce136a16fa` contains immutable historical run evidence and is strictly read-only. It must not be modified, deleted, inspected out of bounds, or self-certified.
4. **Test Harness Decontamination and Isolation:** Test modules must not inject foreign project paths (such as `/home/lee/projects/employee-contract/src`) into `sys.path`. Test execution must remain strictly isolated within `linux-utilities` and declared runtime dependencies. Test scripts must not contain unauthorized bypasses for run directories (`linux-utilities-agent-orch-runs`).
5. **Non-Product Blast Radius:** This repair slice applies strictly to test harness decontamination, regression test restoration, manifest synchronization, and governance documentation. It must introduce zero modifications, regressions, or unauthorized alterations to `sysdiff` C17 source code (`src/sysdiff.c`), Makefile binary targets, or manual pages (`man/sysdiff.1`).
6. **User Journey Manifest Integrity and Synchronization:** Both `tests/user_journeys_manifest.json` and `journeys/user_journeys_manifest.json` must exist as identical parsed JSON objects adhering strictly to `USER_JOURNEYS_MANIFEST_SCHEMA`. All 21 journeys—comprising the 10 workspace abstraction author journeys, 4 core sysdiff utility journeys, and 7 sandbox containment and repair journeys—must be preserved verbatim without omission, reordering, or tampering.
7. **Acceptance Traceability and Command Allowlist:** Every required journey in the manifest must map via `traces_to` to at least one valid acceptance check ID (`AC-1`, `AC-2`, or `AC-3`). No journey may reference an undefined acceptance check, and no contract acceptance check may be left uncovered by required journeys. The manifest `command_allowlist` must remain strictly `["build/sysdiff"]`.
8. **Closed Hazard Taxonomy:** Hazards identified in run `f4ce136a16fa` and this repair slice are classified strictly within the established repository taxonomy: `ORACLE_TAMPERING` (unauthorized bypasses and foreign imports weakening test oracles), `BLAST_RADIUS` (unauthorized modifications outside repair scope), `RESULT_FABRICATION`, `PATH_ESCAPE`, `TOOL_AVAILABILITY`, and `EXECUTION_TIMEOUT`. No unauthorized hazard classes may be introduced.

# Acceptance Checks

- **AC-1** — **Contract Establishment, Document Integrity, and Write Scope Confinement:** The repair contract `docs/repair-f4ce136a16fa-contract.md` is established under required headings (Overview, Problem, Constraints, Acceptance Checks) with at least 120 non-whitespace characters per heading, citing sealed evidence directory `/home/lee/projects/linux-utilities-agent-orch-runs/f4ce136a16fa`. All file mutations across repair steps strictly adhere to declared `allowed_paths` (`docs`, `tests`, `journeys`), ensuring that scratch files are confined to `.agent-orch-scratch/` and zero unauthorized files are created in the workspace root or outside allowed paths.
- **AC-2** — **Test Harness Decontamination and Regression Suite Restoration:** The repair eliminates all foreign repository paths (specifically `/home/lee/projects/employee-contract/src`) from `sys.path` in all test modules (including `tests/test_governed_run_17ca9404991a_repair.py` and `tests/test_governed_workspace_abstraction.py`) and excises unauthorized run-directory bypasses from `tests/test_commissioning_dependencies.py`. The regression test suite in `tests/test_repair_b8bf0e7c59d5.py` passes cleanly under pytest with `test_python_test_harness_decontamination` succeeding, maintaining strict non-product blast radius boundaries and preserving production C17 code.
- **AC-3** — **User Journey Manifest Synchronization, Traceability, and Non-Product Blast Radius:** Both `tests/user_journeys_manifest.json` and `journeys/user_journeys_manifest.json` are maintained as identical, valid JSON objects conforming to `USER_JOURNEYS_MANIFEST_SCHEMA` and preserving all 21 required journeys. Every required journey maps via `traces_to` to an enumerated acceptance check (`AC-1`, `AC-2`, or `AC-3`), with zero unmapped or orphaned acceptance checks. The full test suite passes cleanly, and zero modifications are introduced to `src/sysdiff.c`, Makefile binary targets, or manual pages (`man/sysdiff.1`).
